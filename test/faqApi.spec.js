var assert = require('assert');

describe("FAQ API:", () => {
    it("should bring a list of FAQs", () => {
        const x = 10;
        assert(x).equal(10);
    });

    it('should do something else', () => {

    });

    it('', () => {

    });
})
